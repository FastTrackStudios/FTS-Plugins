[package]
name = "trigger-dsp"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "Drum trigger processing engine — transient detection, velocity extraction, and sample playback"

[dependencies]
fts-dsp = { path = "../../fts-dsp" }
eq-dsp = { path = "../../fts-eq/eq-dsp" }
rustfft = "6.2"

[dev-dependencies]
symphonia = { version = "0.5", features = ["wav", "pcm"] }
