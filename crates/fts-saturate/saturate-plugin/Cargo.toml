[package]
name = "saturate-plugin"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "FTS Saturate — CLAP/VST3 tape saturation plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
fts-plugin-core = { path = "../../fts-plugin-core" }
saturate-dsp = { path = "../saturate-dsp" }
fts-dsp = { workspace = true }
nih_plug = { workspace = true }
