[package]
name = "tape-dsp"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "Tape machine processing engine — encode/decode, flutter, saturation, head bump simulation"

[dependencies]
fts-dsp = { path = "../../fts-dsp" }
