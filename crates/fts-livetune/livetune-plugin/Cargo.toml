[package]
name = "livetune-plugin"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "FTS LiveTune — CLAP/VST3 auto-tune / pitch correction plugin"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
audio-gui = { workspace = true }
fts-plugin-core = { path = "../../fts-plugin-core" }
fts-dsp = { workspace = true }
pitch-dsp = { path = "../../fts-pitch/pitch-dsp" }
livetune-dsp = { path = "../livetune-dsp" }
nih_plug = { workspace = true }
atomic_float = { workspace = true }
serde = { workspace = true }
