[package]
name = "pitch-plugin"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "FTS Pitch — CLAP/VST3 pitch shifter plugin"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
audio-gui = { workspace = true }
fts-plugin-core = { path = "../../fts-plugin-core" }
fts-dsp = { workspace = true }
pitch-dsp = { path = "../pitch-dsp" }
nih_plug = { workspace = true }
atomic_float = { workspace = true }
serde = { workspace = true }
