[package]
name = "reverb-dsp"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "Reverb processing engine — plate, shimmer, hall/room algorithms with input/output EQ"

[dependencies]
fts-dsp = { path = "../../fts-dsp" }
eq-dsp = { path = "../../fts-eq/eq-dsp" }
