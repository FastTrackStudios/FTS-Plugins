[package]
name = "trem-dsp"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Tremolo DSP engine — amplitude modulation with pattern/LFO control"

[dependencies]
fts-dsp = { path = "../../fts-dsp" }
fts-modulation = { path = "../../fts-modulation" }
