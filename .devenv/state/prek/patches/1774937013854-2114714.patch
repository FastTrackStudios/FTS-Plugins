diff --git a/Cargo.lock b/Cargo.lock
index cc8c0e2..60f8976 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -4980,6 +4980,20 @@ dependencies = [
  "realfft",
 ]
 
+[[package]]
+name = "meter-plugin"
+version = "0.1.0"
+dependencies = [
+ "atomic_float 1.1.0",
+ "audio-gui",
+ "fts-plugin-core",
+ "meter-dsp",
+ "meter-ui",
+ "nih_plug",
+ "nih_plug_dioxus",
+ "parking_lot",
+]
+
 [[package]]
 name = "meter-ui"
 version = "0.1.0"
diff --git a/Cargo.toml b/Cargo.toml
index 63a9ff0..4bbdc44 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -85,6 +85,7 @@ members = [
     # Metering library
     "crates/fts-meter/meter-dsp",
     "crates/fts-meter/meter-ui",
+    "crates/fts-meter/meter-plugin",
 
     # Pitch plugin (octave shifter)
     "crates/fts-pitch/pitch-dsp",
diff --git a/Justfile b/Justfile
index 315cc7b..63a7904 100644
--- a/Justfile
+++ b/Justfile
@@ -96,6 +96,7 @@ install-all:
     just install trem-plugin
     just install nam-plugin
     just install midi-guitar-plugin
+    just install meter-plugin
 
 # Install and show REAPER reload instructions
 install-reload plugin=PLUGIN_NAME: (install plugin)
@@ -245,4 +246,5 @@ help:
     echo ""
     echo "Plugins: comp-plugin, eq-plugin, gate-plugin, limiter-plugin,"
     echo "         tape-plugin, delay-plugin, reverb-plugin, trigger-plugin,"
-    echo "         rider-plugin, fts-macros"
+    echo "         rider-plugin, chorus-plugin, pitch-plugin, saturate-plugin,"
+    echo "         trem-plugin, nam-plugin, midi-guitar-plugin, meter-plugin"
diff --git a/crates/fts-eq/eq-plugin/src/editor.rs b/crates/fts-eq/eq-plugin/src/editor.rs
index afb66da..1fde943 100644
--- a/crates/fts-eq/eq-plugin/src/editor.rs
+++ b/crates/fts-eq/eq-plugin/src/editor.rs
@@ -131,7 +131,8 @@ pub fn App() -> Element {
     let text_bright = t.text_bright;
     let _text_dim = t.text_dim;
     let style_label = t.style_label();
-    let style_inset = t.style_inset();
+    let border_subtle = t.border_subtle;
+    let radius_button = t.radius_button;
     let style_card = t.style_card();
     let _style_value = t.style_value();
 
@@ -171,7 +172,10 @@ pub fn App() -> Element {
             // ── Main EQ graph ────────────────────────────────────
             div {
                 style: format!(
-                    "{style_inset} flex:1; min-height:0; position:relative; margin:4px 6px; background:transparent;",
+                    "flex:1; min-height:0; position:relative; margin:4px 6px; \
+                     border:1px solid {border_subtle}; border-radius:{radius_button}; overflow:hidden;",
+                    border_subtle = t.border_subtle,
+                    radius_button = t.radius_button,
                 ),
                 EqGraph {
                     bands: bands_signal,
